{% extends 'base.html.twig' %}

{% block title %}Dogs{% endblock %}

{% block body %}
    <h1>Dogs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dog.id }}</td>
            </tr>
            <tr>
                <th>NameDog</th>
                <td>{{ dog.nameDog }}</td>
            </tr>
            <tr>
                <th>PhotoDog</th>
                <td>{{ dog.photoDog }}</td>
            </tr>
            <tr>
                <th>RaceDog</th>
                <td>{{ dog.raceDog }}</td>
            </tr>
            <tr>
                <th>DescriptionDog</th>
                <td>{{ dog.descriptionDog }}</td>
            </tr>
            <tr>
                <th>AgeDog</th>
                <td>{{ dog.ageDog }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dogs_index') }}">back to list</a>

    <a href="{{ path('app_dogs_edit', {'id': dog.id}) }}">edit</a>

    {{ include('dogs/_delete_form.html.twig') }}
{% endblock %}
