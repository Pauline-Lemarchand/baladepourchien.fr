{% extends 'base.admin.html.twig' %}


{% block title %}Dashboard
{% endblock %}


{% block body %}
{% if is_granted('ROLE_ADMIN') %}
	<div class="dash-front">


		<div class="d-sm-flex align-items-center justify-content-between mb-4">
			<h1 class="h3 mb-0 text-gray-800">Dashboard</h1>

		</div>

		<!-- Content Row -->
		<div class="row dash_home">
			{% for dog in dogs %}


				<!-- Pending Requests Card Example -->

				<div class="card mb-3" style="max-width: 540px;">
					<div class="card border-left-primary shadow h-100 py-2">
						<div class="row g-0">
							<div class="col-md-4">
								<img src="{{ asset('uploads/photoDog/' ~ dog.photoDog ) }}" class="img-fluid rounded-start img_chien" width="200">

							</div>
							<div class="col-md-8">
								<div class="card-body">
									<h5 class="card-title">{{ dog.nameDog }}</h5>
									<p class="card-text">{{ dog.ageDog }}
										ans
										</p>
										<p class="card-text">
										{{ dog.raceDog }}</p>
									<p class="card-text">
										<small class="text-body-secondary">Propriétaire : {{dog.user.firstnameUser}} {{dog.user.nameUser}}</small>
									</p>
								</div>
							</div>
						</div>
					</div>
				</div>

			{% endfor %}

		</div>
	</div>

</div></body></html>{% endif %}{% endblock %}
