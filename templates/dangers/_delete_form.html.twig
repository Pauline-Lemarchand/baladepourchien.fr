<form method="post" action="{{ path('app_dangers_delete', {'id': danger.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ danger.id) }}">
    <button class="btn">Delete</button>
</form>
