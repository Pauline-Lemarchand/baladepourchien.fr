{% extends 'base.html.twig' %}

{% block title %}Dangers{% endblock %}

{% block body %}
    <h1>Dangers</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Zone du danger</th>
                <td>{{ danger.areaDanger }}</td>
            </tr>
            <tr>
                <th>Nom du danger</th>
                <td>{{ danger.nameDanger }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dangers_index') }}">Revenir à la liste </a>

    <a href="{{ path('app_dangers_edit', {'id': danger.id}) }}">Modifier</a>

    {{ include('dangers/_delete_form.html.twig') }}
{% endblock %}
