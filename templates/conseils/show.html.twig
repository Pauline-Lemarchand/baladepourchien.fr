{% extends 'base.html.twig' %}

{% block title %}Conseils{% endblock %}

{% block body %}
    <h1>Conseils</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conseil.id }}</td>
            </tr>
            <tr>
                <th>TitleConseil</th>
                <td>{{ conseil.titleConseil }}</td>
            </tr>
            <tr>
                <th>TextConseil</th>
                <td>{{ conseil.textConseil }}</td>
            </tr>
            <tr>
                <th>PhotoConseil</th>
                <td>{{ conseil.photoConseil }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conseils_index') }}">back to list</a>

    <a href="{{ path('app_conseils_edit', {'id': conseil.id}) }}">edit</a>

    {{ include('conseils/_delete_form.html.twig') }}
{% endblock %}
